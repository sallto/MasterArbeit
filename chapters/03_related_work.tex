% !TeX root = ../main.tex
% Add the above to each chapter to make compiling the PDF easier in some editors.

\chapter{Related Work}\label{chapter:related_work}
\todo{all papers want instsel -> regalloc}

% todo impact of ssa form?



% linear scan dann in Related work und puzzle solving zeugs
\section{Graphcoloring Register Allocation}
\begin{figure}
\centering
\begin{tikzpicture}[
    >=Latex,
    node distance=2.5cm,
    every node/.style={font=\itshape},
    box/.style={draw, minimum width=2.2cm, minimum height=1.1cm, align=center}
]

% Nodes
\node[box] (build) {build IG};
\node[box, right=of build] (colorable) {color};
\node[box, above=1.8cm of colorable] (spill) {spill};

% Arrows
\draw[->] (build) -- (colorable);

% No branch goes up to spill
\draw[->] (colorable) -- node[right, font=\normalfont] {fail} (spill);
\draw[->] (colorable) -- node[below, font=\normalfont] {success} ++(2.5cm,0);

% Spill returns to build IG
\draw[->] (spill.west) -| (build.north);

\end{tikzpicture}
\caption{Simplified overview of the steps in a graph coloring allocator}
\end{figure}

As previously described, graph coloring of interference graphs is equivalent to register allocation~\parencite{Chaitin1981}. 
In general this is an NP hard problem~\parencite{Chaitin1981}. For graphs that are not colorable with $k$ colors, the graph coloring algorithms additionally nedd to iterate between coloring, spilling and coalescing. Where each spill step requires an expensive recomputation of the interference graph\cite{Cooper06}. This makes traditional graph coloring register allocation too slow for just in time compilation~\parencite{Cooper06}. There are multiple improvements proposed to the original algorithm aiming to improve the code quality\todo{study look cgo 2006}.

The cost of rebuilding the interference graph after every spilling step can be avoided by approximating the resulting interference graph using additional metadata~\parencite{Cooper06}. This sacrifices a small amount of code quality for a significant speedup, which makes it more suitable for just in time compilation. Still compared to other methods, the iterative nature of graph coloring is not desirable when prioritizing compilation speed. We leverage the SSA properties to do all spilling as a preprocessing step, and avoid building the interference graph entirely. 

We will consider such SSA-based algorithms such as ours seperately as they have different compilation speed properties, even though they are fundamentally based on graph coloring.
\todo{define graph RA}
\todo{define SSA}
\todo{define k}
% todo define k

\section{Linear Scan Register Allocation}
Linear Scan Allocators fundatmentally of a single scan over the program, finding liveranges and then mapping these live ranges to registers (or memory) \todo{cite og linear scan}.
Although newer research present multiple improvements to the original algorithm\todo{cite shiee cgo}. Linear Scan Allocators are generally faster than graph coloring due to the single scan and no expensive interference graph construction. However, their code quality is generally worse than graph coloring\todo{cite}. While some of the limitations of the original algorithm have been addressed\cite{Wimmer2010}, Linear Scan Allocators fundamentally use a non-optimal approach with increasingly elaborate heuristics or post-processing passes to improve code quality\cite{Wimmer2010}.

Without a preprocessing step it is not possible for a linear scan to find the optimal spill placement around loops, which is particularily problematic as unnecessary spills and reloads in loops are more costly than outside of loops.
Still it is popular choice for JIT Compilers. The original register allocator of TPDE~\cite{tpde} could be considered a heavily modified linear scan allocator.

Our algorithm uses a preprocessing step using SSA properties to find better spill locations, especially around loops. As opposed to linear scan we use a greedy allocator, but with the additional theoretical guarantee that on a unconstrained architecture it will never cause an additional spill.

\section{Trace Register Allocation}
\todo{citations}
For JIT Compilers in particular, trace allocation can be used, which assigns registers on chosen segments incoming control flow, so called traces. This allows for fast algorithm and can use different allocation strategies based on the trace properties. However choosing good traces is non-trivial without runtime profiling information, and the resulting code quality is highly dependent on the trace selection.

As TPDE~\cite{tpde} does not use have access to runtime profiling information, this approach is not suitable for our use case. Our approach allocates across control flow, without runtime information under the assumption that loops will be executed a multiple times.
